spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5436/tasklog}
    username: ${POSTGRES_USER:admin}
    password: ${POSTGRES_PASSWORD:admin123}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: true
    locations: classpath:db/migrations
app:
  jwt:
    secret: ${JWT_SECRET:JQ97P1bhwHojn0Dwz05e6EYjj8Cuj0S3hayUOHvwtUR}
    expiration-ms: ${JWT_EXPIRATION_MS:3600000}
  refresh-token:
    secret: ${REFRESH_TOKEN_SECRET:tUwlxBgabYtQSwAE3v0LlHR4ODOhTj76R0LR7CEGInX}
    expiration-ms: ${REFRESH_TOKEN_EXPIRATION_MS:86400000}
logging:
  level:
    org.springframework.security: DEBUG
    org.springframework.web: DEBUG